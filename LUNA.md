# LUNA 工作指引

## 目標

- 以 LUNA「中」作為日常預設，優先兼顧速度、Token 與正確性。
- 只有在複雜度或錯誤風險足夠高時，才升級到「極高／max」。
- 回覆使用繁體中文，結論優先，避免冗長推理與不必要的檔案掃描。

## 等級策略

### 預設：中

直接使用「中」處理：

- 專案摘要、狀態查詢與一般問答。
- 單一檔案的小型修正。
- UI／390px 手機版檢查。
- 明確、低風險的測試與驗證。

### 升級：極高／max

只有符合以下任一條件才升級：

- FSRS、localStorage、資料遷移或跨檔案資料一致性問題。
- 需要同時分析三個以上模組的架構或流程問題。
- 中等級分析出現互相矛盾、無法確認的結果。
- 可能造成資料遺失、複習紀錄錯誤或重大 UX 回歸。
- 需要設計完整測試矩陣，而不是單一案例驗證。

升級後仍需限制範圍；不要因為任務困難就掃描整個專案。

## 每次任務流程

1. 先讀 `TASK.md` 與 `AGENTS.md`，再只檢查與任務直接相關的檔案。
2. 先確認現況與資料實際結構；實際程式與 JSON 優先於過時的文件數字。
3. 先提出最小可行方案，再進行最小修改。
4. 修改後執行聚焦驗證：
   - 一般程式／UI：`npm run check`
   - 資料或 FSRS：`npm test`
   - 需要完整確認時：`npm run verify`
5. 完成後更新 `TASK.md`，只保留目前狀態與下一步。

## 效率規則

- 不重複讀取已確認的檔案；每個檔案只取必要區段。
- 搜尋優先使用 `rg`，避免列印完整大型檔案。
- 不輸出完整工具紀錄、內部推理或無關程式碼；只回報結論、證據、修改檔案與驗證結果。
- 不因 Token 尚未提供就自行估算為實際用量；若平台沒有 usage，明確標記「未提供」。
- 任務卡住時，先回報阻塞點與已完成檢查，不無限重試。
- 不部署、不新增套件、不修改全域設定，除非使用者明確要求。

## UI 與資料安全

- UI 修改以 390px 寬度為基準，不得產生水平捲動。
- 可操作的觸控目標至少 44px，鍵盤焦點必須清楚。
- 修改資料或 FSRS 前，先確認既有格式與本機紀錄相容性。
- 不以推測資料取代實際 JSON；若文件與資料不一致，指出差異並以實際資料為準。

## 回覆格式

一般任務只回覆：

1. 結論
2. 變更或檢查的重點
3. 驗證結果
4. 尚未完成或下一步

複雜任務才增加風險、替代方案與升級理由。
